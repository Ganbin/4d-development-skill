<!-- Auto-generated by scripts/generate-indexes.py — Do not edit manually -->
<!-- Re-generate: python scripts/generate-indexes.py -->

# REST API Index

> REST API endpoints and configuration

> 36 files in `docs/REST/`

| Title | File | Description |
|-------|------|-------------|
| $asArray | `docs/REST/$asArray.md` | Returns the result of a query in an array (i.e. a collection) instead of a JSON object. |
| $atomic/$atOnce | `docs/REST/$atomic_$atonce.md` | Allows the actions in the REST request to be in a transaction. If there are no errors, the transacti... |
| $attributes | `docs/REST/$attributes.md` | Allows selecting the attribute(s) to get from the dataclass (*e.g.*, `Company(1)?$attributes=name,ci... |
| $binary | `docs/REST/$binary.md` | Pass "true" to save the BLOB as a document (must also pass `$expand={blobAttributeName}`) |
| $catalog | `docs/REST/$catalog.md` | The catalog describes all the dataclasses, attributes, and interprocess (shared) singletons availabl... |
| $clean | `docs/REST/$clean.md` | Creates a new entity set from an existing entity set but without its deleted entities, if any (*e.g.... |
| $compute | `docs/REST/$compute.md` | Calculate on specific attributes (*e.g.*, `Employee/salary/?$compute=sum)` or in the case of an Obje... |
| $directory | `docs/REST/$directory.md` | The directory handles user access through REST requests. |
| $distinct | `docs/REST/$distinct.md` | Returns the distinct values for a specific attribute in a collection (*e.g.*, `Company/name?$filter=... |
| $entityset | `docs/REST/$entityset.md` | After creating an entity set by using `$method=entityset`, you can then use it subsequently. |
| $expand | `docs/REST/$expand.md` | Expands an image stored in an Image attribute (*e.g.*, `Employee(1)/photo?$imageformat=best&$expand=... |
| $filter | `docs/REST/$filter.md` | Allows to query the data in a dataclass or method (*e.g.*, `$filter="firstName!='' AND salary>30000"... |
| $format | `docs/REST/$format.md` | Defines a specific format to return data of a dataclass, (*e.g.*, `$format=looker`) |
| $imageformat | `docs/REST/$imageformat.md` | Defines which image format to use for retrieving images (*e.g.*, `$imageformat=png`) |
| $info | `docs/REST/$info.md` | Returns information about the entity sets currently stored in 4D Server's cache as well as user sess... |
| $lock | `docs/REST/$lock.md` | Locks and unlocks an entity using the pessimistic mechanism. |
| $method | `docs/REST/$method.md` | This parameter allows you to define the operation to execute with the returned entity or entity sele... |
| $orderby | `docs/REST/$orderby.md` | Sorts the data returned by the attribute and sorting order defined (*e.g.*, `$orderby="lastName desc... |
| $querypath | `docs/REST/$querypath.md` | Returns the query as it was executed by 4D Server (*e.g.*, `$querypath=true`) |
| $queryplan | `docs/REST/$queryplan.md` | Returns the query as it was passed to 4D Server (*e.g.*, `$queryplan=true`) |
| $savedfilter | `docs/REST/$savedfilter.md` | Saves the filter defined by $filter when creating an entity set (*e.g.*, `$savedfilter="{filter}"`) |
| $savedorderby | `docs/REST/$savedorderby.md` | Saves the order by defined by `$orderby` when creating an entity set (*e.g.*, `$savedorderby="{order... |
| $singleton | `docs/REST/$singleton.md` | You can directly call exposed functions of your shared singletons through REST. |
| $skip | `docs/REST/$skip.md` | Starts the entity defined by this number in the collection (*e.g.*, `$skip=10`) |
| $timeout | `docs/REST/$timeout.md` | Defines the number of seconds to save an entity set in 4D Server's cache (*e.g.*, `$timeout=1800`) |
| $top/$limit | `docs/REST/$top_$limit.md` | Limits the number of entities to return (e.g., `$top=50`) |
| $upload | `docs/REST/$upload.md` | Returns an ID of the file uploaded to the server |
| $version | `docs/REST/$version.md` | Image version number |
| Calling class functions | `docs/REST/ClassFunctions.md` | You can call data model class functions defined for the ORDA Data Model and singleton class function... |
| About REST Requests | `docs/REST/REST_requests.md` | The following structures are supported for REST requests: |
| Users and sessions | `docs/REST/authUsers.md` | When scalable sessions are enabled (recommended), REST requests can create and use web user sessions... |
| Server Configuration | `docs/REST/configuration.md` | Using standard HTTP requests, the 4D REST Server allows external applications to access the data of ... |
| dataClass | `docs/REST/dataClass.md` | Dataclass names can be used directly in the REST requests to work with entities and entity selection... |
| Getting Server Information | `docs/REST/genInfo.md` | You can get several information from the REST server: |
| Getting Started | `docs/REST/gettingStarted.md` | 4D provides you with a powerful REST server, that allows direct access to data stored in your 4D app... |
| Manipulating Data | `docs/REST/manData.md` | All exposed dataclasses, attributes and functions can be accessed through REST. Dataclass, attribute... |
